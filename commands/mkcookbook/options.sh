# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Wed Jan 30 14:05:54 CST 2013
#
#/ usage: chef-solo:mkcookbook  --scm <git>  --scmurl <>  --target </tmp> [ --pkgname <>]  --build-number <>  --cookbook-version <1.0.0> [ --destdir <>] 

# _rerun_options_parse_ - Parse the command arguments and set option variables.
#
#     rerun_options_parse "$@"
#
# Arguments:
#
# * the command options and their arguments
#
# Notes:
# 
# * Sets shell variables for any parsed options.
# * The "-?" help argument prints command usage and will exit 2.
# * Return 0 for successful option parse.
#
rerun_options_parse() {

    while [ "$#" -gt 0 ]; do
        OPT="$1"
        case "$OPT" in
            --scm) rerun_option_check $# $1; SCM=$2 ; shift ;;
            --scmurl) rerun_option_check $# $1; SCMURL=$2 ; shift ;;
            --target) rerun_option_check $# $1; TARGET=$2 ; shift ;;
            --pkgname) rerun_option_check $# $1; PKGNAME=$2 ; shift ;;
            --build-number) rerun_option_check $# $1; BUILD_NUMBER=$2 ; shift ;;
            --cookbook-version) rerun_option_check $# $1; COOKBOOK_VERSION=$2 ; shift ;;
            --destdir) rerun_option_check $# $1; DESTDIR=$2 ; shift ;;
            # help option
            -?)
                rerun_option_usage
                exit 2
                ;;
            # end of options, just arguments left
            *)
              break
        esac
        shift
    done

    # Set defaultable options.
    [ -z "$SCM" ] && SCM="$(rerun_property_get $RERUN_MODULE_DIR/options/scm DEFAULT)"
    [ -z "$TARGET" ] && TARGET="$(rerun_property_get $RERUN_MODULE_DIR/options/target DEFAULT)"
    [ -z "$COOKBOOK_VERSION" ] && COOKBOOK_VERSION="$(rerun_property_get $RERUN_MODULE_DIR/options/cookbook-version DEFAULT)"
    # Check required options are set
    [ -z "$SCM" ] && { echo >&2 "missing required option: --scm" ; return 2 ; }
    [ -z "$SCMURL" ] && { echo >&2 "missing required option: --scmurl" ; return 2 ; }
    [ -z "$TARGET" ] && { echo >&2 "missing required option: --target" ; return 2 ; }
    [ -z "$BUILD_NUMBER" ] && { echo >&2 "missing required option: --build-number" ; return 2 ; }
    [ -z "$COOKBOOK_VERSION" ] && { echo >&2 "missing required option: --cookbook-version" ; return 2 ; }
    # If option variables are declared exportable, export them.

    #
    return 0
}


# Initialize the options variables to null.
SCM=
SCMURL=
TARGET=
PKGNAME=
BUILD_NUMBER=
COOKBOOK_VERSION=
DESTDIR=


